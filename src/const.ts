import { RatingValue } from './types';

export enum CardPrefix {
  MAIN = 'cities',
  FAVORITES = 'favorites',
  NEARBY = 'near-places'
}

export const RATING_VALUES: RatingValue[] = [5, 4, 3, 2, 1];

